{
  "name": "subagent-orchestrator-ts",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal local orchestrator + parallel subagents pattern, inspired by Google Antigravity 2.0 (I/O 2026).",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "main": "dist/index.js",
  "bin": {
    "subagent-orchestrator": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "tsc -p tsconfig.json && node --test dist/test/*.test.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
