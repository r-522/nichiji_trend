{
  "name": "fable-effort-lab",
  "version": "1.0.0",
  "description": "CLI lab for Claude Fable 5: compare effort levels, stream output, and report token usage / cost / tokenizer deltas",
  "type": "module",
  "bin": {
    "fable-effort-lab": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "dev": "tsc && node dist/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
